BootStrap: docker
From: nipreps/fmriprep:22.0.0

%files
  requirements.txt /opt/requirements.txt

%post
  apt-get -y update
  apt-get -y install gfortran libopenblas-dev liblapack-dev
  pip install --upgrade pip
  pip install -r /opt/requirements.txt

%environment
  export LC_ALL=C